// scripts/hashPasswords.js
// require('dotenv').config({ path: '../.env' }); // si el .env está en la raíz
// const bcrypt = require('bcrypt');
// const mysql = require('mysql2/promise');

// const saltRounds = 10;

// async function migratePasswords() {
//  const connection = await mysql.createConnection({
//    host: process.env.DB_HOST,
//    user: process.env.DB_USER,
//    password: process.env.DB_PASSWORD,
//    database: process.env.DB_NAME
//  });

//  try {
//    // 1. Obtener todos los usuarios con contraseña en texto plano
//    const [users] = await connection.execute(
//      'SELECT Usuario_Id, Usuario_Contraseña FROM usuario'
//    );

//    console.log(`Encontrados ${users.length} usuarios.`);

//    for (const user of users) {
//      const plainPassword = user.Usuario_Contraseña;
//
      // Si ya está hasheada (empieza con $2b$), la saltamos
//      if (plainPassword.startsWith('$2b$')) {
//        console.log(`Usuario ${user.Usuario_Id} ya tiene hash, se omite.`);
//        continue;
//      }

      // Generar hash de la contraseña actual
      //const hashed = await bcrypt.hash(plainPassword, saltRounds);

      // Actualizar en la base de datos
      //await connection.execute(
        //'UPDATE usuario SET Usuario_Contraseña = ? WHERE Usuario_Id = ?',
        //[hashed, user.Usuario_Id]
      //);

      //console.log(`Usuario ${user.Usuario_Id} actualizado.`);
    //}

    //console.log('Migración completada.');
  //} catch (error) {
    //console.error('Error durante la migración:', error);
  //} finally {
    //await connection.end();
  //}
//}

//migratePasswords();